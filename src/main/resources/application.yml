app:
  client:
    tender-pro-url: "https://www.tender.pro/api

spring:
  application:
    name: Zavod_IT
  jackson:
    time-zone: UTC
  liquibase:
    enabled: false
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

springdoc:
  swagger-ui:
    path: "/swagger-ui"

server:
  port: 8080
